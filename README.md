# dev-academy-2021

## HOW TO TEST :
Open the cloned folder in terminal and use command:
$> docker-compose up -d

After build is complete, use command and enjoy:
$> open http://localhost


### DOCKER, PHP, MYSQL
